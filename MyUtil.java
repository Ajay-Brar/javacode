package mypack;
public class MyUtil{
    public int add(int a, int b){
        return (a+b);
    }
    public int greater(int x,int y){
        if(x>y)
        return x;
        else
        return y;
              
    }
}
//for compiling package file we use cammand in cmd javac -d . file name.